language: node_js
node_js:
  - "7"
before_script:
  - npm install
script:
  - echo "antes de la construccion"
  - npm build
  -ls
desplay:
  provider: pages
  skip_cleanup: true
  github_token $GITHUB_TOKEN
  keep_history: true
  on:
  branch: master
